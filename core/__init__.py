"""
core — вычислительное ядро проекта.

ПРАВИЛО (см. занятие 7): этот пакет НЕ импортирует Django, не знает про HTTP, БД и пользователей.
Он принимает параметры, считает и возвращает результат. Его можно запустить из командной строки:

    python -m core input.json output.json

Замените содержимое solver.py на свой метод, сохранив контракт: run(params: dict) -> dict.
"""
from core.solver import VERSION, run  # noqa: F401
