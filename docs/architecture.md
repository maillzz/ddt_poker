# Архитектура проекта <название>

> Шаблон архитектурного документа (12 разделов). Заполняется командой; финальная версия — к предсдаче 2 ноября.
> Диаграммы — draw.io / PlantUML / Mermaid / фото с доски. Объём 6–12 страниц.

## 1. Назначение и контекст
Что делает система, для кого, какая вычислительная задача. Границы: что делаем, чего сознательно не делаем.
Диаграмма C4 уровня 1 (система, пользователи, внешние системы).

## 2. Требования и ограничения
Ключевые требования, ограничения (команда, сроки, стек, среда), риски. Какие качественные атрибуты главные, какие принесены в жертву.

## 3. Контейнеры (C4 уровень 2)
Веб-приложение, БД, брокер, воркер, файловое хранилище, ядро: что где выполняется, как взаимодействует.

## 4. Компоненты веб-приложения (C4 уровень 3, кратко)
Слои: веб-слой (views, API, шаблоны) → сервисы → ядро → инфраструктура. Как соблюдено правило зависимостей.

## 5. Модель данных
ER-диаграмма, сущности и связи, где хранятся большие результаты и почему.

## 6. API
Таблица эндпоинтов (ресурс, метод, запрос, ответ, коды), ссылка на /api/docs. Формат ошибок. Диаграмма состояний задачи.

## 7. Вычислительное ядро
Контракт: вход, выход, ошибки, версия. Эталонные задачи и как проверяется корректность. Ограничения по времени/памяти.

## 8. Безопасность и доступ
Аутентификация, роли, что защищено фреймворком, где секреты, какие угрозы проверены (занятие 14–15).

## 9. Развёртывание
Диаграмма развёртывания, docker-compose, переменные окружения, установка/обновление/откат. Что менять для сети без интернета.

## 10. Эксплуатация
Логи, health-check, бэкап, типовые проблемы и действия администратора.

## 11. Решения (ADR)
Ссылки на docs/adr/. Минимум три: стек, границы системы, очередь/аутентификация.

## 12. Ретроспектива и использование AI
Что бы сделали иначе. Как использовали AI-ассистенты, где они ошибались, как это ловили.
