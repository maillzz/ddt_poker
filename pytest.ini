[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = core/tests tests
# warmup запускается отдельно: pytest warmup -q (пока задачи не решены, тесты красные — это нормально)
